<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    tools:context=".MainActivity">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <View

            android:id="@+id/triangleView"
            android:layout_width="match_parent"
            android:layout_height="400dp"
            android:background="@drawable/ic_triangle_background"
            android:backgroundTint="@color/light_green" />

        <View
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_below="@id/triangleView"
            android:background="@android:color/white">

        </View>
    </RelativeLayout>

    <TextView
        android:id="@+id/register_text_view"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_marginTop="15dp"
        android:gravity="center"
        android:text="LOG IN"
        android:textColor="@color/white"
        android:textSize="20sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/owel_icon"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:layout_marginTop="35dp"
        android:src="@drawable/ic_owel1"
        app:layout_constraintEnd_toEndOf="@id/register_text_view"
        app:layout_constraintStart_toStartOf="@id/register_text_view"
        app:layout_constraintTop_toBottomOf="@id/register_text_view" />
    <TextView
        android:layout_marginBottom="50dp"
        android:textStyle="bold"
        android:gravity="center"
        android:textSize="40dp"
        android:text="Trivia"
        android:textColor="@color/white"
        android:layout_width="120dp"
        android:layout_height="60dp"
        app:layout_constraintTop_toBottomOf="@id/owel_icon"
        app:layout_constraintBottom_toTopOf="@id/ic_download"
        app:layout_constraintStart_toStartOf="@id/owel_icon"
        app:layout_constraintEnd_toEndOf="@id/owel_icon"/>

    <ImageView
        android:id="@+id/ic_download"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_marginTop="55dp"
        android:src="@drawable/ic_download"
        app:layout_constraintEnd_toEndOf="@id/owel_icon"
        app:layout_constraintStart_toStartOf="@id/owel_icon"
        app:layout_constraintTop_toBottomOf="@id/owel_icon"
        app:tint="@color/white" />

    <Button
        android:id="@+id/login"
        android:layout_marginHorizontal="30dp"
        android:drawableLeft="@drawable/ic_facebook"
        android:layout_width="match_parent"
        android:layout_height="65dp"
        android:layout_marginTop="50dp"
        android:backgroundTint="@color/grey"
        android:text="Log in with Facebook"
        android:textColor="@color/black"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="@id/ic_download"
        app:layout_constraintStart_toStartOf="@+id/ic_download"
        app:layout_constraintTop_toBottomOf="@+id/ic_download" />
    <Button
        android:id="@+id/email"
        android:textColor="@color/black"
        android:backgroundTint="@color/white"
        android:layout_marginTop="55dp"
        android:text="Up with Email"
        android:layout_marginHorizontal="138dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="@id/login"
        app:layout_constraintTop_toBottomOf="@id/login"/>

    <TextView
        android:textColor="@color/grey"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="--------OR--------"
        app:layout_constraintBottom_toTopOf="@id/email"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/login"/>
    <Button
        android:textSize="20dp"
        android:backgroundTint="@color/light_blue"
        android:text="GAME NOW"
        android:layout_marginTop="40dp"
        android:layout_marginHorizontal="30dp"
        android:layout_width="match_parent"
        android:layout_height="65dp"
        app:layout_constraintStart_toStartOf="@id/email"
        app:layout_constraintTop_toBottomOf="@id/email"
        app:layout_constraintEnd_toEndOf="@id/email"/>


</androidx.constraintlayout.widget.ConstraintLayout>